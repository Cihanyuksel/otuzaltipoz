export const SKELETON_COUNTS = {
    SMALL: 2,        // < 640px (mobile)
    MEDIUM: 4,       // < 1024px (tablet)
    LARGE: 6,        // < 1536px (desktop)
    EXTRA_LARGE: 8,  // >= 1536px (large desktop)
  } as const;